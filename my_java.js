

//Celsius to Fahrenheit convertation.
//Formula:
//Fahrenheit = Celsius * 9/5 + 32;

//function convertToF(celsius) {
//      var fahrenheit;
//      fahrenheit = celsius * 9 / 5 + 32;
//      return fahrenheit;
//  }


//var str = `umbrella`, i, size = str.length - 1, newStr = ``;


//1. Reverse string via `for` loop.

//for(i = size; i >= 0; i -= 1;){
// newStr += str[i];
// }

//2. Reverse string via native array method `reverse`.

//str.split(``).reverse().join(``);

